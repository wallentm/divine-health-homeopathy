<section class="hero">
  <div class="hero-grid">
    <div class="hero-content">
      <p class="eyebrow">Classical Homeopathy</p>

      <h1>
        Restoring health naturally through individualized care.
      </h1>

      <div class="gold-divider">
        <span></span>
        <img src="/images/dhh-fleur-official.png" alt="" />
        <span></span>
      </div>

      <p class="description">
        Every person has a unique story. Classical homeopathy seeks to
        understand the whole person, supporting the body's natural ability
        to heal through carefully selected individualized remedies.
      </p>
    </div>

    <div class="hero-art">
      <img
        src="/images/botanicals/iris-versicolor-hero-v2.png"
        alt="Iris versicolor botanical illustration"
      />

      <p class="botanical-name">
        <em>Iris versicolor</em>
      </p>
    </div>
  </div>
</section>

<style>
  .hero {
    background: radial-gradient(circle at top, #fcfaf5, #f5efe5);
    padding: 20px 40px 24px;
  }

  .hero-grid {
    max-width: 1200px;
    margin: auto;

    display: grid;
    grid-template-columns: 1.1fr 0.9fr;
    gap: 70px;

    align-items: center;
  }

  .hero-content {
    max-width: 640px;
  }

  .eyebrow {
    margin-bottom: 18px;

    color: var(--gold);

    font-size: 0.92rem;
    letter-spacing: 5px;
    text-transform: uppercase;
  }

  h1 {
    margin-bottom: 20px;

    color: var(--navy);

    font-size: 4rem;
    line-height: 1.02;
  }

  .gold-divider {
    display: flex;
    align-items: center;
    gap: 16px;

    margin-bottom: 28px;
  }

  .gold-divider span {
    width: 86px;
    height: 1px;

    background: var(--gold);
  }

  .gold-divider img {
    width: 18px;
    height: 18px;

    object-fit: contain;
    opacity: 0.72;
  }

  .description {
    color: #596676;

    font-size: 1.28rem;
    line-height: 1.9;
  }

  /* Left here intentionally for future CTA buttons */

  .buttons {
    display: flex;
    align-items: center;
    gap: 14px;

    margin-top: 42px;

    flex-wrap: nowrap;
  }

  .hero-button {
    display: inline-flex;
    align-items: center;
    justify-content: center;

    padding: 15px 22px;

    border-radius: 999px;

    font-size: 0.95rem;
    font-weight: 700;
    line-height: 1;
    text-decoration: none;
    white-space: nowrap;

    transition:
      transform 0.25s ease,
      box-shadow 0.25s ease,
      background 0.25s ease,
      color 0.25s ease;
  }

  .primary {
    background: var(--navy);
    color: white;
    border: 2px solid var(--navy);
  }

  .primary:hover {
    background: #173761;
    transform: translateY(-2px);
    box-shadow: 0 10px 24px rgba(16, 35, 63, 0.18);
  }

  .outline-gold {
    background: transparent;
    color: #7b5f1b;
    border: 2px solid var(--gold);
  }

  .outline-gold:hover {
    background: white;
    transform: translateY(-2px);
    box-shadow: 0 10px 24px rgba(184, 154, 94, 0.18);
  }

  .outline-blue {
    background: transparent;
    color: var(--navy);
    border: 2px solid var(--navy);
  }

  .outline-blue:hover {
    background: white;
    transform: translateY(-2px);
    box-shadow: 0 10px 24px rgba(16, 35, 63, 0.12);
  }

  .hero-art {
    position: relative;

    display: flex;
    flex-direction: column;
    align-items: center;
  }

  .hero-art img {
    width: 100%;
    max-width: 560px;

    margin-top: -20px;

    opacity: 0.28;
    filter: sepia(0.08);

    pointer-events: none;
    user-select: none;
  }

  .botanical-name {
    margin-top: -20px;

    color: #8e8472;

    font-size: 1.05rem;
    letter-spacing: 0.08em;
  }

  /* ---------- Mobile ---------- */

  @media (max-width: 950px) {
    .hero {
      padding: 14px 20px 36px;
    }

    .hero-grid {
      grid-template-columns: 1fr;
      gap: 18px;

      text-align: center;
    }

    .hero-content {
      margin: auto;
    }

    .buttons {
      justify-content: center;
      flex-wrap: wrap;
    }

    .gold-divider {
      justify-content: center;
    }

    .gold-divider span {
      width: 50px;
    }

    .hero-art {
      order: -1;
      margin-top: -12px;
    }

    .hero-art img {
  width: auto;
  max-width: 78vw;
  max-height: 320px;
  margin-top: 0;

  opacity: 0.16;
  object-fit: contain;
}

    .botanical-name {
      margin-top: -4px;

      font-size: 0.95rem;
    }

    .eyebrow {
    margin-bottom: 10px;
}

    h1 {
      margin-bottom: 14px;

      font-size: 2.45rem;
      line-height: 1.08;
    }

    .description {
      font-size: 1.08rem;
      line-height: 1.7;
    }
  }
</style>